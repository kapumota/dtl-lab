### Configuraciones TLA+

#### Configuraciones válidas

- `2shards-1transfer.cfg`;
- `2shards-2transfers.cfg`;
- `3shards-3transfers.cfg`;
- `duplicate-receipt.cfg`;
- `delayed-message.cfg`;
- `timeout-race.cfg`.

#### Configuraciones de mutantes

El subdirectorio `mutants/` selecciona una propiedad objetivo por mutante para conservar un contraejemplo específico.
